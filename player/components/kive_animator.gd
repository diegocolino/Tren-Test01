## Componente que gestionara animaciones cross-state.
## Por ahora es un stub. Se rellenara en fases posteriores si hace falta.
class_name KiveAnimator extends Node

var kive: Node


func _ready() -> void:
	kive = get_parent()
